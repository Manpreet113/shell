# ── Decoration ────────────────────────────────────────────────────────────────
decoration {
    rounding = 6

    blur {
        enabled  = true
        size     = 5
        passes   = 2
        vibrancy = 0.18
    }

    shadow {
        enabled      = true
        range        = 12
        render_power = 3
        color        = rgba(00000066)
    }
}
